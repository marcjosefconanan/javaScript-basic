/*# Esercizio 3

- Crea 3 variabili: `area`, `x` e `y`.
- Se `x` e `y` rappresentano i lati di un rettangolo, assegna loro due valori numerici e usa la variabile `area` per calcolare l'area del rettangolo.
- Stampa in console il valore dell'area */

//--------------------------------------------------------------------

//ASNWER

let x = 10;
let y = 15;

let area = x + y; 

console.log (area); 

//other answer: 

/*
let x, y, area;

x = 10;
y = 15;

area = x + y; 

console.log (area); 
*/

//i gave x and y numerical values and with the use of console.log () i calculated it by adding the two numerical values and naming it area.
