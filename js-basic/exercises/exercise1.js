/*Crea le variabili necessarie per specificare marca, modello, 
anno di produzione e nome del proprietario di un'auto e assegna loro un valore.
Dichiarale usando let e const dove ritieni opportuno.
Dichiara un'ulteriore variabile che servirà per sapere se è un'auto nuova o usata
Utilizza i tre tipi di variabili studiati finora, number, boolean e string
Se utilizzi un nome composto da più di una parola, ricorda di utilizzare la notazione camel case
Ricorda che è buona prassi utilizzare l'inglese per il naming delle variabili
*/

//--------------------------------------------------------------------

//ASNWER

const carBrand = "Ferrari"; // const = (variable type) carBrand = (variable name) "=" = (defining the variables value) "Ferrari" = (variable value in this case a "string" data type value.)
const carModel = "testa rossa"; // "testa rossa" = variable name = (data type "string")
const productionYear = 2015; //  2015 = variable name = (data type number) 
let ownersName = "Marc Josef Conanan"; //let = (a variable type that is mainly used for interchangeable variables)
let ferrariAreNew = true; // true = variable name = (data type boolean)
let ferrariAreUsed = false; // false = variable name = (data type boolean)

//i put const in the 3 variable carBrand,carModel,productionYear. because their the ones that won't change that often unlike the other variables.
